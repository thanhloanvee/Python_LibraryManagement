# ================================================================
# Library Management System - FastAPI Backend
# Requirements
# Python 3.12+ compatible (tested on 3.14)
# Use flexible lower-bound pins so pip resolves pre-built wheels
# ================================================================

# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.1

# Auth
# python-jose is unmaintained and lacks Python 3.14 wheels;
# PyJWT is the modern, actively maintained replacement.
PyJWT[crypto]>=2.8.0
# passlib is unmaintained and incompatible with bcrypt 4.x; use bcrypt directly.
bcrypt>=4.0.0
python-multipart>=0.0.9

# Validation & Settings
# Pydantic >=2.8 ships pre-built pydantic-core wheels for Python 3.14
pydantic>=2.8.0
pydantic-settings>=2.3.0
email-validator>=2.1.1

# Templating & static files
jinja2>=3.1.4
python-multipart>=0.0.9
aiofiles>=23.2.1
itsdangerous>=2.1.2

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0

# Dev tools
ruff>=0.4.4
